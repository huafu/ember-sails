{{#if isEditing}}
  {{view Ember.Select
  content=choices
  optionValuePath="content.id"
  optionLabelPath="content.label"
  selection=selected
  prompt='-- N/A --'}}
{{else}}
  {{#if value.recordLabel}}
    {{value.recordLabel}}
  {{else}}
    {{value.id}}
  {{/if}}
{{/if}}
